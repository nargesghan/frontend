import React from 'react';

interface Props {
  // define your props here
}

const Page: React.FC<Props> = (props) => {
  return (
    <div>
      employer landing page
    </div>
  );
}

export default Page;